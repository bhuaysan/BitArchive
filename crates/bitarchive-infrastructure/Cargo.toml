[package]
name = "bitarchive-infrastructure"
description = "BitArchive infrastructure layer: HTTP, downloads, and the versioned component store"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
bitarchive-application = { path = "../bitarchive-application" }
bitarchive-domain = { path = "../bitarchive-domain" }
rusqlite = { workspace = true }
sha2 = { workspace = true }
ureq = { workspace = true }
zip = { workspace = true }
